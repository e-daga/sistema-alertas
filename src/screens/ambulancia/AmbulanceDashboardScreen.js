import React from "react";
import ResponderDashboardScreen from "../common/ResponderDashboardScreen";

export default function AmbulanceDashboardScreen({ navigation }) {
  return <ResponderDashboardScreen navigation={navigation} variant="ambulancia" />;
}