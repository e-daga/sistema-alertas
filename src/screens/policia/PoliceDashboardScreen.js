import React from "react";
import ResponderDashboardScreen from "../common/ResponderDashboardScreen";

export default function PoliceDashboardScreen({ navigation }) {
  return <ResponderDashboardScreen navigation={navigation} variant="policia" />;
}